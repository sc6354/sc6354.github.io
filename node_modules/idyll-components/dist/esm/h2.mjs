import _extends from '@babel/runtime/helpers/extends';
import React from 'react';

const generateId = (headerText = '') => {
  return headerText.toString().trim().replace(/\s+/g, '-').toLowerCase();
};

const GenerateHeaders = props => {
  const {
    idyll,
    hasError,
    updateProps,
    size,
    children = [],
    ...attributeProps
  } = props;
  const headerText = children.join('');
  const HeaderTag = `h${size}`;

  if (!attributeProps.id) {
    attributeProps.id = generateId(headerText);
  }

  return /*#__PURE__*/React.createElement(HeaderTag, attributeProps, children);
};

class H2 extends React.PureComponent {
  render() {
    return /*#__PURE__*/React.createElement(GenerateHeaders, _extends({
      size: "2"
    }, this.props));
  }

}

H2._idyll = {
  name: 'H2',
  tagType: 'open',
  children: ['My Header Size 2']
};

export { H2 as default };
